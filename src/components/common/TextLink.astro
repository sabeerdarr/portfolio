---
/** Arrow text link used for "view all" style navigation. */
export interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class:list={['text-link', className]}><slot /></a>
